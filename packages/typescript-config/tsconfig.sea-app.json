{
    //
    // TypeScript configuration for applications that use Node's single
    // executable application feature.  These apps must provide a CommonJS entry
    // point.
    //
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig.app.json",
    "compilerOptions": {
        "module": "Node16",
        "moduleResolution": "Node16"
    }
}
